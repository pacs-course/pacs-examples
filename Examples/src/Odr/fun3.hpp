#pragma once
namespace myspace {
    /*
    This is a declaration of a function fun3 that takes a double and returns a double.
    The definition of this function is provided in the corresponding source file (b.cpp).
    The function calls fun1 and fun2, which are defined in the header file (fun1.hpp).
    */
    double fun3(double x);
}