/*! A simple function to illustrate some basic features of pybind11. 

It adds all integers between two given integers and returns the sum*/
#include <algorithm> // for std::swap
int sumInt(int m, int n);

