An example of accumulation of roundoff errors in a sum. We use Kahan summation to reduce errors (see Wikipedia...).

COmpile with

make CPPFLAGS=-DREAL=float

if you want floats instead of double

If you want long double
make CPPFLAGS='"-DREAL=long double"'

remember the two quotes!
